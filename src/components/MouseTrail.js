import React from 'react';
import './MouseTrail.css';

function MouseTrail(trailers) {
    return (
        <>
        {/* {trailers.map((trailer, i) => <Trailer icon={object} key={i} />)} */}
        </>
    )
}

export default MouseTrail;